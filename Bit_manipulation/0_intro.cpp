#include<iostream>
using namespace std;
#include<bits/stdc++.h>

/*
a>>1     -- right shift operator 1001>>1 = 0100 , 1001>>2=0010 
if a>>x after operation we will get no. = a/2^x;


a<<1     -- left shift operator  1001<<1 = 10010 , 1001<<2=100100;
if a<<x after operation we will get no. = a*2^x;

& ans operation  any zero - overall 0;
| or operation  any one - overall 1;
^ xor operation  both same - overall 0;


//and operation application-
check even and odd;
if(n&1==0){
    cout<<"this is even";
}
else{
    cout<<"its odd no. ";
}
// odd no me last bit always set bit hoti h bhaiyo or pyari bheno understand it na ? very well;
 










*/